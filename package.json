{
  "name": "equilife",
  "private": true,
  "version": "1.1.0",
  "scripts": {
    "dev": "concurrently -n api,web -c blue,green \"npm run dev:api\" \"npm run dev:web\"",
    "dev:api": "cd back && yarn dev",
    "dev:web": "cd front && yarn dev",
    "release:tag": "bash ./scripts/release-tag.sh"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
